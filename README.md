# VectorVoyage
VectorVoyage is a way to query 100% efficient accross unknown data set with Elasticsearch on Document ingest we dynamically create a array of most relevant document keywords which is then appended as vector to the document on ingest, Using Elasticsearch, FastAPI and React in python and typescript
